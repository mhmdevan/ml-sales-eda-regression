# v2-specific ignores (place inside v2/.gitignore) — if you keep v2 as its own submodule/repo

# Raw data (Kaggle download)
data/raw/
!data/raw/.gitkeep

# Processed parquet layer (generated by pipeline)
data/processed/
!data/processed/.gitkeep

# DuckDB database
duckdb/
*.duckdb

# ML artifacts
models/
output/

# MLflow store
mlruns/

# Local caches
__pycache__/
.pytest_cache/

.dvc
